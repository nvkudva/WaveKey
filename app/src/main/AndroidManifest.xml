<?xml version="1.0" encoding="utf-8"?>
<!--
Copyright (C) 2012 The Android Open Source Project
modified
SPDX-License-Identifier: Apache-2.0 AND GPL-3.0-only
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- WaveKey departs from HeliBoard here, deliberately and visibly
         (PLAN.md §3.3). Upstream requests neither of the two below.

         RECORD_AUDIO is the product: the IME process listens.

         wavekey:internet-scoped — INTERNET exists only so models can be
         downloaded, and every component that touches the network runs in the
         `:ui` process (WorkManager's workers are pinned there by the
         Configuration.Provider in App.kt). Android has no way to scope a
         permission to one process, so this is enforced by keeping networking
         components out of the keyboard process and asserted by
         ManifestProcessSplitTest.

         Two opt-in exceptions to the on-device-only rule live behind their own
         switches on the Privacy-breaking features screen, both off by default:
         Google voice typing (SpeechRecognizer — the audio is recorded and sent
         by Google's process, not ours) and Google Password Manager fill (the
         platform's inline autofill). Neither touches the network from this
         process; both hand data to Google through system IPC, which is the part
         the user is being asked to consent to. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application android:label="@string/english_ime_name"
            android:name="helium314.keyboard.latin.App"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:allowBackup="true"
            android:defaultToDeviceProtectedStorage="true"
            android:directBootAware="true"
            android:enableOnBackInvokedCallback="true"
            android:localeConfig="@xml/locales_config"
            tools:remove="android:appComponentFactory"
            tools:targetApi="33">

        <!-- WaveKey: the refiner runs in its own process so a native OOM
             or a MediaPipe crash inside a 0.5B model cannot take the keyboard
             down mid-sentence, and so the keyboard's memory ceiling stops being
             set by an optional feature (PLAN.md §3.4). Not exported: the only
             client is us. -->
        <service
            android:name="com.vboard.app.llm.LlmRefinerService"
            android:exported="false"
            android:process=":llm" />

        <!-- WaveKey: WorkManager is initialized on demand (App implements
             Configuration.Provider) instead of by androidx.startup, so the
             keyboard process does not host the downloader's machinery. Removing
             just this initializer leaves the startup provider available for
             anything else that needs it. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- WaveKey: the download worker hosts its foreground
             notification in WorkManager's own service. That service ships
             without a foregroundServiceType and targetSdk 34+ refuses to start
             an untyped foreground service, so the type is merged in here — as is
             the `:ui` process, which is what actually keeps the download out of
             the keyboard. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:process=":ui"
            tools:node="merge" />

        <!-- Services -->
        <service android:name="LatinIME"
                android:label="@string/english_ime_name"
                android:permission="android.permission.BIND_INPUT_METHOD"
                android:directBootAware="true"
                android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data android:name="android.view.im" android:resource="@xml/method_dummy" />
        </service>

        <service android:name=".spellcheck.AndroidSpellCheckerService"
                 android:label="@string/spell_checker_service_name"
                 android:permission="android.permission.BIND_TEXT_SERVICE"
                 android:exported="true">
            <intent-filter>
                <action android:name="android.service.textservice.SpellCheckerService" />
            </intent-filter>
            <meta-data android:name="android.view.textservice.scs"
                    android:resource="@xml/spellchecker" />
        </service>

        <!-- Activities -->
        <!-- WaveKey: the one activity that can show the microphone dialog; an
             IME cannot ask for a runtime permission itself. Invisible, kept out
             of recents, and gone as soon as the dialog is answered. -->
        <activity
            android:name="helium314.keyboard.voice.MicPermissionActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:exported="false" />

        <!-- WaveKey: the settings UI deliberately stays in the keyboard
             process. Pinning it to `:ui` would put the two halves of the app on
             opposite sides of a SharedPreferences instance that is not
             multi-process safe, and every setting the keyboard reads live would
             start drifting. The network split does not depend on it: WorkManager
             runs its workers in `:ui` (App.setDefaultProcessName) and its
             foreground service is pinned below. -->
        <activity android:name="helium314.keyboard.settings.SettingsActivity"
                android:theme="@style/platformActivityTheme"
                android:icon="@mipmap/ic_launcher"
                android:windowSoftInputMode="adjustResize"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- separate activity that's used internally, only necessary in case the launcher icon is disabled on Android < 10 -->
        <activity android:name="helium314.keyboard.settings.SettingsActivity2"
            android:theme="@style/platformActivityTheme"
            android:icon="@mipmap/ic_launcher"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <!-- intent filter for opening .dict files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/octet-stream" android:scheme="content" android:host="*" android:pathPattern=".*\\.dict" />
            </intent-filter>
        </activity>

        <activity android:name=".spellcheck.SpellCheckerSettingsActivity"
                android:theme="@style/platformActivityTheme"
                android:label="@string/spell_checker_service_name"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name="helium314.keyboard.keyboard.emoji.EmojiSearchActivity" android:exported="false"
            android:theme="@style/TransparentActivityTheme" android:excludeFromRecents="true" android:noHistory="true"
            android:windowSoftInputMode="adjustResize|stateAlwaysVisible" android:configChanges="orientation|screenSize"/>

        <!-- Broadcast receivers -->
        <receiver android:name="SystemBroadcastReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_INITIALIZE" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver android:name="DictionaryPackInstallBroadcastReceiver" android:exported="false">
            <intent-filter>
                <action android:name="helium314.keyboard.dictionarypack.aosp.UNKNOWN_CLIENT" />
            </intent-filter>
        </receiver>

        <!-- for pasting files from internal clipboard -->
        <provider android:name="helium314.keyboard.latin.database.ClipboardContentProvider"
            android:authorities="@string/clipboard_provider_authority"
            android:enabled="true"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/clipboard_provider_path"/>
        </provider>

        <!-- necessary for sending mail attachments, remove when data gathering phase is done (end of 2026 latest) -->
        <provider android:name="helium314.keyboard.settings.screens.gesturedata.GestureFileProvider"
            android:authorities="@string/gesture_data_provider_authority"
            android:enabled="true"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/gesture_data_path"/>
        </provider>
    </application>

    <queries>
        <!-- To detect other IMEs -->
        <intent>
            <!-- changed to * as it's supposed to help with finding other keyboard apps, see https://github.com/HeliBorg/HeliBoard/issues/1340 -->
            <!--<action android:name="android.view.InputMethod" />-->
            <action android:name="*" />
        </intent>

        <!-- To detect names of installed apps -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
</manifest>
